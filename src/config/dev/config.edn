{
 :server {:port 3500 :join? false}
 :db {:spec 
      {:maximum-pool-size  3
       :adapter            "postgresql"
       :username           "lunch"
       :database-name      "lunch"
       :server-name        "localhost"
       :port-number        5432}
      }
 }
